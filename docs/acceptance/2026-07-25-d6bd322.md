# MD-013 公开验收记录：d6bd322

本记录只包含虚构数据、聚合计数和脱敏结论，不包含 Cookie、工作区 locator、凭据、原始日志或私人组合。

## 候选

| 字段 | 证据 |
|---|---|
| 候选 commit SHA | `d6bd322cbb2e8f713cd3ec82156c5948e8cd8e56` |
| 公开 HTTPS URL | `https://demo.wuxie233.com` |
| 部署与复核时间 | 2026-07-25 09:00-09:06 CST |
| `/health.version` | 与候选 SHA 精确一致 |
| 执行人 | OpenCode Lead |
| 浏览器与系统 | 系统 Chrome，Linux；Playwright desktop 1280x800 与 mobile 375x812 touch/reduced-motion |

候选绑定结论：`通过`。

## 自动化浏览器

执行命令使用公开 origin、候选 SHA 和系统 Chrome；命令不含 secret。`pnpm test:e2e` 共 12 项，12 项通过，用时 35.7 秒。

| 项目 | 结果 | 脱敏证据摘要 |
|---|---|---|
| desktop Chromium 1280x800 | 通过 | 6/6 |
| mobile 375x812 + touch + reduced motion | 通过 | 6/6 |
| S0-S10 普通随机体验路径 | 通过 | 首次引导、工作区、分析、长笺、历史均可达 |
| 分析刷新 / 回访恢复 | 通过 | 刷新后恢复，回访跳过首次引导 |
| 历史不可变重放 | 通过 | 从普通历史入口读取同一结果 |
| 横向溢出 | 通过 | 两个项目的关键屏断言均通过 |
| console / page / network failure gate | 通过 | 无非白名单失败 |
| 公开路径 / sourcemap / client secret 扫描 | 通过 | 敏感路径和 `.map` 返回 404，公开资产未命中 secret 形态 |

本次普通路径确定性覆盖 `supported`、`limited` 和 `unavailable`。`observation_only` 没有普通公开确定性入口，未验证。

## 状态与截止矩阵

| 场景 | 普通入口 | 可观察终态 | 结果 |
|---|---|---|---|
| supported | 四项约束均已知后发起复盘 | 长笺显示“证据支持” | 通过 |
| limited | 一项约束保持 unknown 后发起复盘 | 长笺显示“有限分析” | 通过 |
| unavailable | 把一个示例持仓改为无同资产 fixture 的代码后发起复盘 | 显示“分析不可用，可重试”，无结果入口 | 通过 |
| observation_only | 无普通公开确定性入口 | 未验证 | 未验证 |
| 外部供应商 / 模型失败 | 无普通公开确定性故障注入 | 未验证 | 未验证 |
| 180 秒硬截止及迟到结果隔离 | 无普通公开确定性入口 | 未验证 | 未验证 |

90/180 秒提示文字由浏览器旅程覆盖，但这不替代真实 180 秒截止和任务清理证据。

## 隐私与公开面

| 检查 | 证据 | 结果 |
|---|---|---|
| 工作区 Cookie 属性 | 浏览器断言 Secure / HttpOnly / SameSite=Lax / Path=/ 且无 Domain；不记录值 | 通过 |
| URL、DOM、Web Storage | 浏览器隐私面断言 | 通过 |
| 服务日志敏感形态 | 08:55 CST 后按 Cookie、Authorization、Bearer、供应商 key、locator 和持仓形态扫描，命中 0 | 通过 |
| 公开静态资源 | 敏感路径、sourcemap 和 client secret 自动扫描 | 通过 |
| 原始截图 | 本次验收未使用截图导入，也未产生或保留原始用户截图 | 不适用 |

## Purge 演练

使用过期的虚构最小 sentinel 验证正式 `mandong-purge.service`，不把 sentinel 作为产品状态结果：

- 执行前：workspace、draft、analysis run、analysis event、history 各 1，tombstone 为 0。
- 执行后：上述五类数据均为 0，tombstone 为 1。
- `mandong-purge.service` 的 `Result=success`、`ExecMainStatus=0`。
- timer 为 enabled/active，下一次运行已排期；SQLite `integrity_check=ok`。

## 回滚演练

| 步骤 | release ID | `/health.version` | 数据库证据 | 结果 |
|---|---|---|---|---|
| 部署候选 | `d6bd322` | 精确匹配 | `integrity_check=ok` | 通过 |
| 回滚上一 release | `e44cedf` | 精确匹配 | 回滚期间写入的虚构 workspace、draft、run、event、history 均保持 1 | 通过 |
| 恢复候选 | `d6bd322` | 精确匹配 | sentinel 经正式 purge 清理，五类 live 数据均为 0 | 通过 |

回滚使用 live DB，没有恢复迁移前快照。旧 release 遇到未知未来 schema 的生产故障注入未执行；fail-closed 恢复路径由仓库部署验证覆盖，不冒充真实主机证据。

## 归档与资源

- `./deploy/validate.sh` 通过，fault harness 覆盖 traversal、控制字符、额外根路径、重复项、symlink、hardlink、FIFO、device 和 required type 错误。
- `pnpm check`、31 个测试文件共 220 项、`pnpm build` 均通过。
- Node 只监听 `127.0.0.1:8787`；复核时 `mandong.service` 为单一主进程、`NRestarts=0`。
- Playwright 退出后没有残留 Playwright 或其临时 Chrome profile 进程。
- 两个 task-owned sentinel 的 live workspace 均已删除，只保留不可恢复删除所需 tombstone。

## 结论

- 当前候选已在公开 HTTPS URL 部署，可达范围的公开浏览器验收和部署演练通过。
- `observation_only`、外部供应商 / 模型真实失败、180 秒硬截止与迟到任务隔离仍未获得普通公网运行证据。
- 因上述未验证项，完整四状态与硬截止公开验收结论为`部分通过`，不得表述为全矩阵通过。
