# 满懂 2 至 3 分钟演示脚本

状态：`录制前脚本`。目标时长 `2:35`，允许在 `2:20-2:50` 内剪辑。画面以最终真实产品为主；字幕和局部放大可以辅助理解，但不能用合成界面代替功能。

## 演示目标

评委在一次普通首次使用中看到：

1. 用户如何确认随机体验组合和四项约束；
2. 单个分析 agent 如何以可观察的真实阶段处理输入和证据；
3. 东方观象主题版复盘报告如何先给出可读结论，再翻到理性证据；
4. 证据不足时产品如何缩小结论，而不是伪造正常结果。

不设置评委专用路由。正常态和有限态都必须从普通入口、可见场景选择或真实用户操作到达。

## 录制前条件

- 使用版本化、明确标注的示例数据。任何出现持仓、金额或建议的画面持续显示 `Simulated` 或等价标记。
- 熊猫“兜兜”只属于东方观象主题；其它锁定主题使用各自的占位吉祥物。所有角色只改变表现，不改变证据、风险或建议。
- 主路径从已提交且有同资产证据的候选池随机生成体验身份。录制可固定可见种子以复现画面，但仍须标注随机体验与示例/缓存边界。
- 透明缓存证据显示真实来源、观察时间、获取时间、证据截止时点和缓存状态；没有可核对供应商响应时只标为测试 fixture。
- 分析等待态只显示单个分析 agent 的真实阶段、覆盖、等待、重试、失败和结构化产物状态，不展示思维链。
- 脚本中的按钮、状态和字段必须存在于最终分支。没有实现的镜头直接删除。
- 全程隐藏密钥、管理页、终端凭据、真实账户、真实持仓和个人研究记录。

## 分镜

| 时间 | 真实画面与操作 | 旁白 | 必须证明 |
|---|---|---|---|
| `0:00-0:12` | 从普通公开 URL 打开满懂，直接进入主题选择；停在东方观象与三个锁定预览 | “市场信息很多，真正困难的是判断它和自己的持仓、期限与流动性有什么关系。满懂把这些信息整理成一份可以核对的复盘报告。” | 唯一可用主题与锁定边界，非营销落地页 |
| `0:12-0:34` | 选择“先随便看看”，生成随机体验身份；停留在醒目的体验标识、持仓摘要、四项约束和种子/缓存边界 | “我从普通入口使用一份可复现的随机体验组合。它不是我的真实持仓；主题也只改变表达，不改变证据、风险判断或建议。” | 随机体验标识、四项约束、主题边界 |
| `0:34-0:58` | 进入主界面，打开侧栏和仓位/身份页；展示完整体验持仓、来源时点，并把一项约束设为 `unknown/not decided` | “体验身份仍可检查和修改。四项约束允许明确选择未知，系统不会用默认值悄悄补齐。” | 主界面、工作区状态、完整输入、unknown |
| `0:58-1:13` | 返回主界面并打开发起确认；停留在当前输入、最新完整交易日、90 秒目标和 180 秒硬截止 | “开始前，满懂会冻结本次持仓、约束和证据边界。之后修改会产生新快照，不会改写旧复盘。” | 二级确认、不可变快照、时限边界 |
| `1:13-1:35` | 启动分析；等待态依次显示一个已完成真实阶段和一个等待或有限重试状态 | “这里展示的是单个分析 agent 的真实阶段、覆盖和失败状态，不是思考动画，也不表示预测更准。” | 真实任务事件、单 agent、非思维链 |
| `1:35-1:54` | 展开一条透明缓存证据，停留在来源、观察时间、获取时间、截止时点和缓存标识 | “示例使用可复现的同资产缓存证据。这里区分市场观察时间、系统获取时间和证据截止时点；它不是此刻实时报价，也不替代供应商运行验收。” | 可追溯、时点、缓存、非实时声明 |
| `1:54-2:13` | 生成完成后，纵向浏览东方观象主题版报告正面；停在主要变化、组合相关性、方向性建议和风险提示 | “复盘报告先说今天最重要的变化，为什么和这个组合有关，以及下一步应关注的方向。它不提供精确金额、比例、价格或交易时点。” | 主题版复盘报告、方向性边界 |
| `2:13-2:28` | 横向拖动三维翻面；再演示可见的“查看证据”按钮替代。展开主结论对应的确认输入、派生关系、证据和未知项 | “翻到理性证据背面，同一结论能回到确认输入、派生结果或带日期证据。观察、派生和生成解释彼此可区分。” | 翻面交互、非手势替代、同一分析版本 |
| `2:28-2:43` | 返回普通入口选择证据过期或缺失的公开示例；展示有限态、受影响结论和补数动作 | “同一条普通路线也能进入有限态。缺失或过期证据不会被补成当前值；满懂保留能确认的内容，并明确哪些结论暂时不能下。” | 真实降级、受影响范围、恢复动作 |
| `2:43-2:50` | 快速回到工作区，显示本次记录与旧记录的快照/截止时点区分；结束在产品名和真实 URL | “每次复盘都保留自己的快照和证据时点，让用户看懂结论，也能回头核对它为什么成立。” | 30 天历史、版本和公开 URL |

## 旁白约束

- 使用“帮助理解”“方向性建议”“当前证据支持”等可核验措辞。
- 不说“预测准确”“高置信上涨”“避免亏损”“专业投资意见”或“自动调仓”。
- 不把缓存数据称为实时行情，不把供应商风格示例称为供应商集成。
- 不让兜兜预测吉凶、承诺结果、催促交易或替用户作决定。
- 不声称“Qoder 独立完成产品”或“所有代码由 AI 生成”。
- 没有真实研究证据时，不说“用户都需要”“访谈证明”或具体提升百分比。

## 现场降级

现场网络或外部服务失败时，仍从同一个普通入口选择已提交的透明缓存场景。演示者要主动说明缓存的来源、时点和限制，并展示有限态。不能切换到隐藏评委页、开发者开关、录制假响应或伪造当前值。

分析生成目标为 90 秒、180 秒硬截止。脚本可以使用赛前生成且版本一致的透明缓存结果控制时长，但必须显示它对应的快照、证据截止时点和缓存状态，不得冒充现场实时生成。

## 录制验收

- [ ] 总时长不超过 3 分钟，每个关键字段能停留到可读。
- [ ] 首次使用不是跳过式预置结果，随机体验持仓、四项约束、体验标识和快照边界均可见。
- [ ] 主画面来自最终产品，鼠标和触控动作与实际响应一致。
- [ ] `Simulated` 在所有含持仓、金额或建议的示例画面可见。
- [ ] 分析等待态至少展示一次真实阶段、覆盖或产物状态。
- [ ] 东方观象叙事正面和理性证据背面引用同一分析版本。
- [ ] 兜兜只出现在东方观象；其它主题使用独立占位吉祥物，且不改变理性结果。
- [ ] 三维翻面有可见按钮替代，核心信息不依赖 hover 或动画。
- [ ] 有限态从普通产品路线到达，并说明仍能确认、不能判断和如何恢复。
- [ ] 无凭据、真实个人金融数据、未脱敏用户记录或供应商越界主张。
