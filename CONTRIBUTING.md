# Contributing to FinLens

## Before Starting

阅读 `PRODUCT.md`、`CONTEXT.md`、`docs/specs/mvp.md` 和 `docs/decisions/` 下所有已接受 ADR。确认工作对应一个 GitHub Issue，并检查该 Issue 的前置依赖已经完成。

Issue 是执行票据，不是替代产品契约的地方。新需求若改变用户行为、范围或验收，先更新规格；新方案若形成难以逆转或反直觉的技术决策，先提交 ADR。不要提前选择未确认的技术栈或供应商。

## Issue Contract

一个可执行 Issue 至少包含：

- 用户或评审者要完成的场景；
- 当前问题及其证据；
- 明确的范围和非目标；
- 依赖的 Issue 编号；没有依赖时写 `None`；
- 可观察、可重复的验收条件；
- 正常路径、失败路径、隐私影响和建议边界；
- 完成后需要保存的测试、截图、录屏或审计证据。

优先拆成端到端 tracer bullet。单独的层级工作只有在能解除明确依赖，并带有独立契约测试时才适合作为 Issue。

## Change Workflow

1. 在 Issue 中确认依赖、验收条件和非目标。
2. 创建聚焦分支，只修改完成该 Issue 所需内容。
3. 先建立覆盖关键正常路径和失败路径的验证。
4. 实现最小的完整纵向行为。
5. 在桌面和 375px 视口核对核心流程、证据追溯与隐私边界。
6. 更新相关文档和持久 `AGENTS.md` 知识。
7. 提交 Pull Request，并用 `Closes #<issue>` 关联票据。

## Product Safety Review

每个 Pull Request 都要回答以下问题：

- 是否出现精确金额、比例、价格或时点的个性化交易指令？
- 每项方向性建议是否能追溯到确认输入和证据？
- 未知、过期、冲突和失败是否会诚实缩小结论？
- 主题是否只影响表达，而不改变金融推理？
- 模拟数据与真实用户数据是否在界面和数据流中清楚分离？
- 公开路由、URL、日志、资源、分析事件和分享预览是否不含个人金融数据？
- 截图提取结果是否保持为待确认草稿？

发现越界时不要依赖免责声明补救，应修正输出或停止该分析路径。

## Verification Evidence

Pull Request 应列出实际运行的检查及结果，并附上与风险相称的证据。用户流程变更至少需要正常路径和相关失败路径；市场适配器变更需要类型化状态的契约测试；隐私变更需要公开路由、日志和分享预览检查；响应式交互变更需要 375px 触控及键盘替代验证。

测试 fixture 必须虚构、最小且明确标注。不得上传真实持仓截图、完整账号、凭据、原始组合载荷或包含个人信息的录屏。

## Commit and Review Scope

保持提交可构建、可测试、可独立回退。Pull Request 描述应说明关联 Issue、用户可见变化、已验证的失败模式、隐私影响和遗留限制。无关重构、格式化和依赖升级应拆开提交和评审。
