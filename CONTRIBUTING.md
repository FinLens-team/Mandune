# 为满懂贡献

## 开始前

按 [`AGENTS.md`](AGENTS.md) 的顺序阅读产品、领域、设计、Demo、分析和供应商集成契约，以及 [`docs/decisions/`](docs/decisions/) 下当前有效的 ADR。再确认工作对应一个 GitHub Issue，且阻塞依赖已经完成。

Issue 是执行票据，不替代产品契约。改变用户行为、范围或验收条件时，先更新相应规格；形成难以逆转或反直觉的技术选择时，先记录 ADR。供应商文档中未核验的字段、权限或行为不得通过实现猜测补齐。

## Issue 契约

一个可执行 Issue 至少包含：

- 用户场景和当前问题；
- 最小端到端范围与明确非目标；
- 阻塞 Issue；没有依赖时写 `None`；
- 可重复的正常、有限、失败和恢复验收；
- 隐私、证据和建议边界；
- 关闭前需要保存的测试、截图、录屏或审计证据。

优先交付纵向切片。单独的数据层、模型层或视觉层工作，只有在解除明确依赖并提供独立契约证据时才应成为票据。当前建议图见 [`docs/tickets/README.md`](docs/tickets/README.md)。

## 变更流程

1. 在 Issue 中确认依赖、范围、非目标和验收证据。
2. 创建聚焦分支，只修改完成该 Issue 所需的文件。
3. 先建立能观察正常路径和关键降级路径的验证。
4. 实现最小的完整用户结果，保持供应商与模型边界可替换。
5. 对界面工作验证桌面、375px 触控、键盘操作和减少动态效果；对分析工作验证证据引用、未知项、硬截止和恢复状态。
6. 更新受影响的规格、文档地图和就近 `AGENTS.md` 中可复用的已验证知识。
7. 提交 Pull Request，并使用 `Closes #<issue>` 关联票据。

## 安全检查

每个 Pull Request 都要回答：

- 每项物质性结论能否回到确认输入、派生结果或已核验的带日期证据？
- 缺失、过期、含糊、冲突、不支持、限流和失败是否会缩小或停止相关结论？
- 输出是否避免精确金额、比例、价格、买卖时点、收益保证和代客操作？
- 主题、兜兜或模型表达是否改变了证据、计算、覆盖、风险或建议？
- 提取模型是否只在披露后临时接收原图，分析模型是否只接收确认后的完整结构化持仓？原图、身份、账户、工作区凭据和供应商密钥是否未进入分析模型，完整私人持仓是否未进入公开资源、URL、默认日志、分析事件或 fixture？
- PandaAI 或 Bocha 的能力声明是否有运行证据，Bocha 搜索摘要是否仅作为候选线索？
- Agent 观测台是否只展示真实阶段和结构化产物，而不展示思维链或制造预测把握？

免责声明不能修复越界输出。发现越界时应修正输出、缩小分析或停止该路径。

## 验证证据

Pull Request 应列出实际运行的检查及结果。用户流程变更至少覆盖正常路径和相关有限/失败路径；供应商变更覆盖鉴权、字段与时点、空结果、限流、超时、冲突和日志脱敏；模型变更覆盖版本化结构、畸形输出、越界内容、有限重试、180 秒硬截止和失败降级；界面变更覆盖 375px、键盘、触控和非动画替代。

fixture 必须虚构、最小、版本化并明确标注。不要提交真实持仓截图、完整账户资料、Bearer token、私人组合载荷或含个人信息的录屏。研究材料还需取得同意并完成脱敏。

当前仓库没有可运行命令。只有命令已经提交并在干净环境中通过后，才可以把它写入 [`README.md`](README.md) 和 [`AGENTS.md`](AGENTS.md)。

## 提交与评审

保持提交可验证、可独立回退。Pull Request 描述应说明关联 Issue、用户可见变化、已验证的失败模式、隐私影响和遗留限制。无关重构、格式化和依赖升级应拆开评审。
